# 更新日志

> 在 2026-08-05 将核心解析和 demux 实现迁至 `libaribtlv` 之前，项目使用
> `tlvdemux` 命名空间。相关条目以 **旧 `tlvdemux` 实现** 标记；这只是历史名称，
> 并非独立产品线。

## [0.3.7] - 2026-08-31

- 录制 seek 的 head discovery 不再仅凭轨道目录完成；进入稀疏 probe 前还必须观察到带时间戳的
  候选 playback access unit。即使信令早于首个媒体 access unit，也能保留录制的真实
  normalization origin。
- 将 5.1 GiB 可变码率录制及其 `452.985098s` 目标加入 16 MiB budget 的直接回归，并增加
  可定位已观测 RAP 的 restart/input offset 的 probe diagnostics。
- 稀疏 probe 会先跨过时长线性 estimate 周边不含媒体的有界区间，再向后调整；同时用浏览器
  兼容的音轨选择，在同一个 16 MiB budget 内验证 4.8 GiB 录制的 `110.390227s` 目标。

## [0.3.6] - 2026-08-31

- 让显式录制 seek 独占 probe、landing、append 和精确 A/V commit；自动层切换、韧性恢复及
  过期的异步 playback intent 不再能够改写用户请求的 `currentTime` 或冻结的入口时钟。
- 以 remuxer 已确认的 coded A/V 区间作为 landing commit 依据，同时仍要求与浏览器真实缓冲
  区间相交；允许 Chromium 不精确的 HEVC RAP 边界，但不会接受确实只位于目标之后的媒体。
- 在同一个 16 MiB budget 内使用有界的 1 MiB 读取、稳定 RAP 观测、更早的 AAC preroll 和
  幂等的 worker 轨道重放；失败时会在取消前保存阶段、入口判定、已提交/浏览器缓冲区间及
  字节消耗等诊断信息。
- WASM 构建时刷新生成的 CMake metadata，避免 Nix 中 Emscripten 或 Ninja store path 更新后
  继续复用失效的旧工具链路径。

## [0.3.5] - 2026-08-29

- 在 AAC 配置变化、reposition、层切换和音轨复用过程中保持完整的绝对
  `SourceBuffer.timestampOffset`，不再用相对连续性修正覆盖 fresh recorded 的入口映射。
- 让 `0xf300` 的 SDR→HDR 过渡始终保持单一视频 generation，同时将选中的 `0xf310`
  音频从 6ch 切为 2ch；使用绝对 `-714638us` offset 将音频 splice 两侧都映射到
  `16.224050s`，且不触发降雨层切换或 recovery seek。

## [0.3.4] - 2026-08-29

- 增加公开的 video-unavailable resilience 模式：连续三次前向 RAP 恢复仍无新画面后，
  将缓冲要求切为纯音频；只有后续 RAP 的帧真正提交到 compositor 后才恢复视频。
- 增加有界的录制/Live 切换契约、运行时 `activeSourceBuffers` 验证，以及不改变视频区域
  几何的稳定 `TLV_VIDEO_UNAVAILABLE` demo 状态。
- 显式录制 seek 的目标位于视频损坏区间时，延长损坏前最后一帧或复制首个稳定 RAP 作为
  静止填补帧，在保持 AAC 连续及原始 RAP 时间戳不变的同时，使 `currentTime` 始终停留在
  用户请求的准确位置。
- 复用正式 landing 后的 demuxer，禁止第二次 reposition、重新扫描或扩大共享 16 MiB budget；
  同时统一 demo 模块版本，并修复 recovery logger 在 controller 初始化前访问它的顺序错误。

## [0.3.3] - 2026-08-28

- 将自动双视频录制的时间线定义为首选轨与降雨轨 presentation range 的并集，公开其
  start/end，并让时长、起播、显式 seek 与 MSE timestamp 共用同一个时间原点。
- 当前选中层存在明确的 playback-damage seek 时仅执行一次；prefetch 阶段发现的 damage
  会保留到播放抵达该区间或发生停顿后再触发。
- 在可恢复的 source damage 前后保持 AAC output 连续，并阻止视频损坏重置独立音频
  timeline，修复所提供录制在 Chromium 中触发的 `DEMUXER_UNDERFLOW`。

## [0.3.2] - 2026-08-28

- 在安装任一 SourceBuffer 前，将 fresh recorded playback 的首个公共 A/V 区间原子映射到
  timestamp 0，且不修改 media-element clock。
- 保留显式降雨 splice offset；只有在 16 MiB startup budget 用尽后仍无法形成公共 A/V
  入口时，才返回 `MSE_STARTUP_NO_COMMON_AV`。

## [0.3.1] - 2026-08-28

- 为 Live 播放增加独立的 flow-control 入口：首个有效公共 A/V 区间即使从
  timestamp 0 之后开始也可正常接收，并仅在达到配置的 Live 起播缓冲后将
  media clock 对齐到该区间。
- Live 流无法形成任何公共 A/V 区间时仍以 16 MiB 无进展上限停止读取，并为
  Live 正常对齐和有界失败两条路径增加直接回归测试。

## [0.3.0] - 2026-08-28

- 增加公开 browser SDK 模块，统一承载 16 MiB 有界录制 seek、MSE output queue、
  轨道/层级选择、严格 recorded Range source、时长探测、live 输入合并、播放损坏恢复及 Worker RPC。
- demo 已迁移到公开模块并删除重复 lifecycle helper。package publish 仍是需明确执行的 release
  动作；在文档所列 DPlayer 常规 npm cutover 与 consumer 验收完成前，该批次不算完成。
- Worker 边界现在保留调用方持有的 `Uint8Array`，避免录制 seek 在 probe/landing 间复用缓存时因
  `ArrayBuffer` 已 detached 而失败。
- 从手动降雨层切回自动时，系统现在会在下一个可用 RAP 主动恢复健康的首选 A/V 组合，取代尚未完成的
  手动切换，并将首次播放入口建立前的手动切换映射到 startup playback entry。

## [0.2.4] - 2026-08-26

Commit range: `7aa53cb..HEAD`

- 增加 Annex-B HEVC 色彩信息和 HDR 静态元数据解析；将 `libaribtlv` 集成更新至
  0.6.0，并补充时间戳与 fMP4 回归测试。
- 使用原生 3D LUT 和硬件三线性采样优化 WebGPU HLG-to-SDR 转换，同时记录仅重解释
  元数据为何不能成为浏览器端有效的 tone mapping 路径。
- 通过 WASM/TypeScript API 和 inspector 的 JSON 输出公开 IP 数据流、传输层 NTP
  时钟、TLV 网络信息与地址映射表、原始信令、未知 descriptor 及信令消息。

## [0.2.3] - 2026-08-19

Commit range: `2e93df2..7aa53cb`

- 增加正常播放控制、seek 计算工具及相关回归测试覆盖。
- 改进 HDR 呈现策略、视频信号、静态元数据处理和色彩分析。
- 在 worker 和 WASM 模块中增加 MSE 输出能力、格式及状态处理。

## [0.2.2-rc1] - 2026-08-15

Commit range: `1adff6b..HEAD`

- 增加 HLG-SDR 视频信号控制、串流中的视频属性实时更新，以及保留 BT.2020 原色和矩阵的
  SDR 重解释路径。
- 增加 C++/WASM HLG-SDR tone mapping LUT 和 WebGPU 亮度专用渲染器，修正 HLG 中承载的
  SDR 内容，同时避免分别放大 RGB 通道造成过饱和。
- 增加色彩路径的 native、WASM 和实际样本回归测试。

## [0.2.1] - 2026-08-15

Commit range: `e960417..1adff6b`

- 忽略首个有效 TLV 包之前可恢复的填充字节，避免启动重同步变成 MSE 错误。
- AAC 缺帧时保持解码时间线连续；LATM StreamMuxConfig 变化时（包括
  5.1 ↔ 立体声）重新初始化 MSE 音频。
- 增加初始 TLV 重同步、AAC 前向时间戳间隙、重复音频配置切换，以及基于实际样本的
  WASM 音频配置切换回归测试。

## [0.2.0] - 2026-08-15

Commit range: `7c47b47..55796ec`

- 引入独立的 HEVC/AAC MSE 轨道 muxer、Chromium coded-frame policy 测试、更安全的
  跨层 seek，以及播放损坏上报与恢复指引。
- 扩展 VideoToolbox 诊断，并改进面向广播播放的视频层重新探测和 seek 行为。

## [0.1.11] - 2026-08-14

Commit range: `239cb25..9a34f78`

- 增加更丰富的字幕元数据、字幕渲染与选择、音频资产组选择，以及视频/音频图层的
  协同切换、预备与同步。

## [0.1.10] - 2026-08-14

Commit range: `4e353a4..60369de`

- 将命令行工具整合为统一可执行文件，并改进静态链接。
- 在 MP4 输出中保留 HEVC 色彩信息，并增加带 splice 事件的 MSE 音频轨切换。

## [0.1.9] - 2026-08-11

Commit range: `3c6d800..e232e11`

- 增加 `tlvanalyze` 和 `tlvdemux-pipe` 工具，以及原生构建/发布自动化。
- 将核心解析和 demux 实现迁至 `libaribtlv`，并将 `tlvdemux` 集成迁移至
  `aribtlv` 的公开类型和命名空间。

## [0.1.8] - 2026-08-03

Commit range: `d1ff955..ed55f37`

- 通过丢弃不前进 DTS、在安全 composition 边界切分片段、保留源 timescale、阻止无效
  前导 HEVC 图像进入解码器，强化 MSE 时序和 coded-frame 处理。
- 修正 ARIB B60 access unit 时间戳推导、闰秒锚定、字幕参考时间验证，以及 MPU
  一致性检查。

## [0.1.7] - 2026-08-03

Commit range: `49c5a63..73451a1`

- 增加 `MseAppendQueue`、SourceBuffer 生命周期收尾、媒体片段验证，以及浏览器队列示例。

## [0.1.6] - 2026-08-03

Commit range: `4b7e9e1..18f44e4`

- 增加观众参与通知、MPT/MH-AIT 快照、MH-SDT/MH-TOT 解析、扩展事件，以及相关的
  数据广播支持。

## [0.1.5] - 2026-08-01

Commit range: `c187e51..e66a821`

- 合并实时流读取，并为提升播放吞吐量记录和测量 WASM 输入与内存行为。

## [0.1.4] - 2026-08-01

Commit range: `ab4f4a6..4cdc744`

- 增加 WebKit media-plane 回退，改进 MSE 回调处理和演示文档，并调整键盘与视口行为。

## [0.1.3] - 2026-08-01

Commit range: `3363bdc..7b47136`

- 扩展 ARIB 信令支持，并在 Safari 演示中启用 `ManagedMediaSource`。

## [0.1.2] - 2026-08-01

Commit range: `eaef4cf..27cd7ae`

- 增加流事件、字幕 passthrough、应用 descriptor 与 profile，以及数据广播字幕集成。

## [0.1.1] - 2026-07-31

Commit range: `c06b2f8..e4684b0` — **旧 `tlvdemux` 实现**

- 增加 WASM 演示播放、seek 和 Range 支持；引入首版 HEVC MSE remuxer、实时起播缓冲、
  RASL 处理、VideoToolbox 探测和录制 seek 支持。
- 增加应用资源管理、广播时钟/节目事件、广播资源处理，以及 B62 字幕资源和时序元数据。

## 初始开发 - 2026-07-19 至 2026-07-30

Commit range: `050f800..02e6b9b` — **旧 `tlvdemux` 实现**

- 建立 MMT/TLV demuxer、compressed-IP/MMTP 解析、服务选择、时间戳映射、资源限制、
  CMake 打包、CLI 检查、音频元数据、录制索引和 seek 基础能力。
- 增加 B62 字幕资源和时序元数据、应用/数据资产解析与提取、解压、时长探测，以及初始
  Emscripten/WASM 绑定。
